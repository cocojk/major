cwd ( current working directory)

pwd (현재 일하는 디렉토리 보여주기)

touch a (새로만들기)
ls -l (디렉토리 내용보기)
cd (change directory)

프로세스 - 동작하고 있는 것



cat (내용 읽기)

ls -i
i 노드 넘버  / 파일 이름 

ln a b  ( b 가 a 하드 링크 )
-> i 노드 넘버 같다 !! 




/A/B/a.c

root 디렉토리 entry에서 A를 가르키는 i -node를 찾아서 A를 연다
A를 연담에 A entry에서 B를 가르키는 i -node를 찾아서 B를 연다.

chmod o ->> other
chmod g --> 그룹
chmod u+x tel
--> 지나갈려면 execute 해야함
--> tel 파일에서 user 영역에서 excute 가능하게함
chmod 2진수/2진수/2진수 
ex) chmod 754 -> rwxr-xr--





ln -s a b
심볼릭 링크
--> i 노드는 다르다 !
--> 경로명이 들어와있다.
처음 l 표시
--> excute 권한이 있다.

root 이용
1. sudo 앞에 붙이기
2. su 이후 비밀번호
root 비밀번호 변경
sudo passwd root
jobs 
실행중


ls -lR(모든 디렉토리 보기 ~~)
cat /tmp/a | grep jay(jay관련된거)| wc -l (word count)->> output을 다음 파일의 input으로 바꾸기
cat- 파일 보기 
| (pipe) output이 input이 되는 것 one way commnication chanel!


vi sig.c
./sig&
kill -SIGUSR1 5438 (5438에게 SIGUSR1 signal을 보내기 !)
kill %1 - sigkill
fage fault  피지컬 메모리(페이지 크기만큼 나눠서 그 영역에 할당),storage device(피지컬 메모리가 넘치면 저장)
more /etc/fstab

